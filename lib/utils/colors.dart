import 'dart:ui';
import 'package:flutter/material.dart';

// --- TMK School Branding Palette ---

// Primary Background & Surfaces
const kBrandDark = Color(0xFF1A1A1B);      // Deep Charcoal (The logo background)
const kBrandSurface = Color(0xFF2D2D2E);   // Slightly lighter for cards/sections

// Primary Accents (The Sun and Spirit)
const kBrandOrange = Color(0xFFE65100);    // Vibrant Orange from the logo
const kBrandGold = Color(0xFFFFD54F);      // Golden Yellow from the logo text

// Secondary Accents (The Water and Progress)
const kBrandBlue = Color(0xFF4FC3F7);      // Sky Blue from the river/mountains
const kBrandWhite = Color(0xFFF5F5F5);     // Off-white for clean text

// Functional Colors
const kBrandSuccess = Color(0xFF4CAF50);   // For passing scores
const kBrandError = Color(0xFFD32F2F);     // For incorrect answers